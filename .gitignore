node_modules/
*.log

# Local data should live under ~/.local/share/cowork-zap, but ignore these in
# case anyone runs with project-relative overrides.
auth/
inbox/
outbox/
.env

# VitePress (the built site is committed under /docs and is NOT ignored).
website/.vitepress/cache/
website/.vitepress/dist/
